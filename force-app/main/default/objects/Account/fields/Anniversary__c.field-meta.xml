<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Anniversary__c</fullName>
    <externalId>false</externalId>
    <formula>TEXT(IF(
DATE(YEAR(Plan_Sold_Date__c), MONTH(TODAY()), DAY(TODAY())) &gt;=
DATE(YEAR(Plan_Sold_Date__c), MONTH(Plan_Sold_Date__c), DAY(Plan_Sold_Date__c)),
YEAR(TODAY()) - YEAR(Plan_Sold_Date__c),
YEAR(TODAY() - 1) - YEAR(Plan_Sold_Date__c))) +
(IF
(NOT(ISNULL(Plan_Sold_Date__c)),
IF(YEAR(TODAY()) - YEAR(Plan_Sold_Date__c) = 1,&quot; Year&quot;,&quot; Years&quot;), 
null))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Anniversary</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
