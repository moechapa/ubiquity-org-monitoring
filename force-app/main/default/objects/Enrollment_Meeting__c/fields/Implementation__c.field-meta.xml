<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Implementation__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Implementation that is in progress when enrollment meeting is conducted. Only needs to be linked if the plan is in the implementation stage.</description>
    <inlineHelpText>Implementation that is in progress when enrollment meeting is conducted. Only needs to be linked if the plan is in the implementation stage.</inlineHelpText>
    <label>Implementation</label>
    <referenceTo>Implementation__c</referenceTo>
    <relationshipLabel>Enrollment Meetings</relationshipLabel>
    <relationshipName>Enrollment_Meetings</relationshipName>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
