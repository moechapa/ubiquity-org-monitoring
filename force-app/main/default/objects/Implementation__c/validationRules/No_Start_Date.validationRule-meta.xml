<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>No_Start_Date</fullName>
    <active>true</active>
    <description>Ensures that if an imp is set to &quot;no start,&quot; the date is completed (button should be used)</description>
    <errorConditionFormula>ISPICKVAL(Implementation_Status__c,&quot;No Start&quot;)

&amp;&amp;

ISNULL(No_Start_Date__c)

&amp;&amp;

ISCHANGED(Implementation_Status__c)</errorConditionFormula>
    <errorDisplayField>Implementation_Status__c</errorDisplayField>
    <errorMessage>When setting an implementation&apos;s status to &quot;No Start,&quot; please use the button at the top of the page.</errorMessage>
</ValidationRule>
