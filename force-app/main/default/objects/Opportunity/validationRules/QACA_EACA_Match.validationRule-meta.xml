<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>QACA_EACA_Match</fullName>
    <active>true</active>
    <errorConditionFormula>ISPICKVAL(StageName, &quot;Closed Won&quot;)

&amp;&amp;

ISCHANGED(StageName)

&amp;&amp;

((CONTAINS(TEXT(AutoEnrollment__c),&quot;QACA&quot;)
&amp;&amp;
CONTAINS(TEXT( Safe_Harbor__c ),&quot;EACA&quot;))
||
(CONTAINS(TEXT(AutoEnrollment__c),&quot;EACA&quot;)
&amp;&amp;
CONTAINS(TEXT( Safe_Harbor__c ),&quot;QACA&quot;)))

&amp;&amp;

NOT(CONTAINS(TEXT(Service_Option__c),&quot;Simply&quot;))</errorConditionFormula>
    <errorMessage>Ubiquity plans cannot mix EACA with QACA - verify Auto-Enrollment and Safe Harbor</errorMessage>
</ValidationRule>
