<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Proposal_Type__c</fullName>
    <description>Used for reporting back to Principal for Simply</description>
    <externalId>false</externalId>
    <formula>IF(ISPICKVAL(Stage_Flag__c,&quot;&quot;),&quot;Proposal Only&quot;,
IF(ISPICKVAL(Stage_Flag__c,&quot;Review&quot;), &quot;Proposal Only&quot;, &quot;Proposal with Action&quot; ))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Used for reporting back to Principal for Simply</inlineHelpText>
    <label>Proposal Type</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
