<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Response_To_Parent_Child_Cases_only</fullName>
    <active>true</active>
    <description>Response to Parent status used on Child Cases only</description>
    <errorConditionFormula>AND ( ISCHANGED( Status ) ,
ISPICKVAL(Status, &quot;Response to Parent Case&quot;),
(ISBLANK( ParentId )))</errorConditionFormula>
    <errorDisplayField>Status</errorDisplayField>
    <errorMessage>&quot;Response to Parent Case&quot; is only used on Child Cases to trigger a status change on the Parent Case</errorMessage>
</ValidationRule>
