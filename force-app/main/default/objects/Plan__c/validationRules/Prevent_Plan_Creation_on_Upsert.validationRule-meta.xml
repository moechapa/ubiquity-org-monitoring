<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Prevent_Plan_Creation_on_Upsert</fullName>
    <active>false</active>
    <description>Prevents new Plan records from being created during Upsert. Add the alias of every new user trained in Upserts.</description>
    <errorConditionFormula>AND(
ISNEW(),
$User.Alias = &quot;sbula&quot;
)</errorConditionFormula>
    <errorMessage>Prevented creation of new Plan record.</errorMessage>
</ValidationRule>
